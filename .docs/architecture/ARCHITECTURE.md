# 🏗️ Vercel Deployment Architecture

## 📊 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         GITHUB REPOSITORY                            │
│                     discover-influencers (main)                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                     Monorepo Structure                       │    │
│  │  ┌─────────────┐              ┌─────────────┐              │    │
│  │  │  apps/web   │              │  apps/api   │              │    │
│  │  │  (Next.js)  │              │  (Express)  │              │    │
│  │  └─────────────┘              └─────────────┘              │    │
│  │         │                             │                      │    │
│  │         └─────────────┬───────────────┘                      │    │
│  │                       │                                      │    │
│  │                pnpm workspace                                │    │
│  └────────────────────────────────────────────────────────────┘    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ git push
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         VERCEL PLATFORM                              │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Auto-Deploy Trigger                         │  │
│  └─────────────────────────┬─────────────────────────────────────┘  │
│                            │                                         │
│              ┌─────────────┴─────────────┐                          │
│              │                           │                          │
│              ▼                           ▼                          │
│  ┌────────────────────┐      ┌────────────────────┐                │
│  │   WEB PROJECT      │      │   API PROJECT      │                │
│  │ (Next.js App)      │      │ (Express Server)   │                │
│  │                    │      │                    │                │
│  │ Root: apps/web     │      │ Root: apps/api     │                │
│  │                    │      │                    │                │
│  │ ┌────────────────┐ │      │ ┌────────────────┐ │                │
│  │ │ Build Process  │ │      │ │ Build Process  │ │                │
│  │ ├────────────────┤ │      │ ├────────────────┤ │                │
│  │ │ 1. pnpm install│ │      │ │ 1. pnpm install│ │                │
│  │ │ 2. Next.js build│ │     │ │ 2. Prisma gen  │ │                │
│  │ │ 3. Standalone  │ │      │ │ 3. Migrations  │ │                │
│  │ │    output      │ │      │ │ 4. TypeScript  │ │                │
│  │ └────────────────┘ │      │ └────────────────┘ │                │
│  │                    │      │                    │                │
│  │ ┌────────────────┐ │      │ ┌────────────────┐ │                │
│  │ │ Environment    │ │      │ │ Environment    │ │                │
│  │ ├────────────────┤ │      │ ├────────────────┤ │                │
│  │ │ API_URL        │ │      │ │ DATABASE_URL   │ │                │
│  │ └────────────────┘ │      │ │ JWT_SECRET     │ │                │
│  │                    │      │ │ CORS_ORIGIN    │ │                │
│  │ ┌────────────────┐ │      │ └────────────────┘ │                │
│  │ │ Deployment     │ │      │                    │                │
│  │ ├────────────────┤ │      │ ┌────────────────┐ │                │
│  │ │ Edge Network   │ │      │ │ Deployment     │ │                │
│  │ │ Global CDN     │ │      │ ├────────────────┤ │                │
│  │ │ SSR/SSG        │ │      │ │ Serverless     │ │                │
│  │ └────────────────┘ │      │ │ Functions      │ │                │
│  │                    │      │ │ Auto-scaling   │ │                │
│  │ 📱 URL:            │      │ └────────────────┘ │                │
│  │ your-web.vercel.app│      │                    │                │
│  └────────────────────┘      │ 🔌 URL:            │                │
│                              │ your-api.vercel.app│                │
│                              └────────────────────┘                │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                        MONITORING                              │ │
│  │  • Build Logs  • Function Logs  • Analytics  • Errors         │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                             │
                             │ Database Connection
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    PRODUCTION DATABASE                               │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │   PostgreSQL (Vercel Postgres / Supabase / Neon)             │  │
│  │                                                                │  │
│  │   Tables:                                                      │  │
│  │   • users          • people                                   │  │
│  │   • categories     • refresh_tokens                           │  │
│  │                                                                │  │
│  │   Migrations: Applied automatically during build              │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### User Visits Web App

```
1. User Browser
   └─► https://your-web.vercel.app
       │
       ▼
2. Vercel Edge Network
   └─► Serves Next.js App (SSR/SSG)
       │
       ▼
3. Browser Renders
   └─► Makes API calls to API URL
       │
       ▼
4. API Request
   └─► https://your-api.vercel.app/api/...
       │
       ▼
5. Vercel Serverless Function
   └─► Executes Express.js handler
       │
       ▼
6. Database Query
   └─► PostgreSQL via Prisma
       │
       ▼
7. Response
   └─► JSON data back to browser
       │
       ▼
8. UI Updates
   └─► React re-renders with data
```

---

## 🚀 Deployment Flow

### Code Push Triggers Deployment

```
Developer                  GitHub                  Vercel
    │                         │                       │
    │  git push origin main   │                       │
    ├────────────────────────►│                       │
    │                         │                       │
    │                         │  Webhook Trigger      │
    │                         ├──────────────────────►│
    │                         │                       │
    │                         │                   ┌───┴───┐
    │                         │                   │ Build │
    │                         │                   │ Queue │
    │                         │                   └───┬───┘
    │                         │                       │
    │                         │                  ┌────▼─────┐
    │                         │                  │ Web Build│
    │                         │                  └────┬─────┘
    │                         │                       │
    │                         │                  ┌────▼─────┐
    │                         │                  │ API Build│
    │                         │                  └────┬─────┘
    │                         │                       │
    │  Build Success          │                   ┌───▼────┐
    │◄────────────────────────┼───────────────────┤ Deploy │
    │  Deployment URL         │                   └────────┘
    │                         │                       │
    │                         │                   ✅ Live!
```

---

## 📦 Build Process Details

### Web App Build

```
┌─────────────────────────────────────┐
│     1. Clone Repository             │
│        ├─ apps/web                  │
│        ├─ apps/api                  │
│        └─ pnpm-workspace.yaml       │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     2. Install Dependencies         │
│        cd ../..                     │
│        pnpm install --frozen-lockfile│
│        • All workspace deps          │
│        • Shared packages             │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     3. Build Next.js                │
│        pnpm --filter web build      │
│        • Compile TypeScript          │
│        • Generate pages              │
│        • Optimize assets             │
│        • Create standalone output    │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     4. Deploy to Edge               │
│        • Upload to CDN               │
│        • Configure serverless        │
│        • Set up routing              │
└─────────────────┬───────────────────┘
                  │
                  ▼
                ✅ Live
```

### API Build

```
┌─────────────────────────────────────┐
│     1. Clone Repository             │
│        ├─ apps/api                  │
│        └─ pnpm-workspace.yaml       │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     2. Install Dependencies         │
│        cd ../..                     │
│        pnpm install --frozen-lockfile│
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     3. Generate Prisma Client       │
│        prisma generate              │
│        • Create type-safe client     │
│        • Generate query methods      │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     4. Run Migrations               │
│        prisma migrate deploy        │
│        • Apply pending migrations    │
│        • Update database schema      │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     5. Compile TypeScript           │
│        tsc                          │
│        • Compile src to dist         │
│        • Generate JS files           │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│     6. Deploy Serverless            │
│        • Create function bundles     │
│        • Configure routes            │
│        • Set environment vars        │
└─────────────────┬───────────────────┘
                  │
                  ▼
                ✅ Live
```

---

## 🔐 Environment Variables Flow

```
┌──────────────────────────┐
│  Vercel Dashboard        │
│  Project Settings        │
│  Environment Variables   │
└───────────┬──────────────┘
            │
            │ Injected at Build/Runtime
            │
    ┌───────┴───────┐
    │               │
    ▼               ▼
┌─────────┐   ┌──────────┐
│   WEB   │   │   API    │
│         │   │          │
│ • API   │   │ • DB URL │
│   URL   │   │ • JWT    │
│         │   │ • CORS   │
└─────────┘   └──────────┘
    │               │
    │               │
    ▼               ▼
┌─────────┐   ┌──────────┐
│ Client  │   │ Server   │
│ Side    │   │ Side     │
│ (Public)│   │ (Secret) │
└─────────┘   └──────────┘
```

---

## 🌍 Global Infrastructure

```
                    ┌─────────────────┐
                    │  Vercel Global  │
                    │  Edge Network   │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Americas   │    │    Europe    │    │  Asia-Pacific│
│              │    │              │    │              │
│ US-East-1    │    │ EU-West-1    │    │ AP-SE-1     │
│ US-West-1    │    │ EU-Central-1 │    │ AP-NE-1     │
└──────────────┘    └──────────────┘    └──────────────┘
```

**Benefits**:
- ⚡ Low latency worldwide
- 🔄 Automatic failover
- 📈 Auto-scaling
- 🌐 Global CDN

---

## 📊 Resource Allocation

### Web App (Next.js)
- **Compute**: Serverless Functions (Node.js runtime)
- **Storage**: Static assets on CDN
- **Memory**: 1024 MB per function
- **Timeout**: 10 seconds (Hobby) / 60s (Pro)
- **Concurrent**: Unlimited

### API (Express)
- **Compute**: Serverless Functions (Node.js runtime)
- **Memory**: 1024 MB per function
- **Timeout**: 10 seconds (Hobby) / 60s (Pro)
- **Concurrent**: Auto-scaling
- **Cold Start**: ~100-300ms

### Database
- **Connection**: Via connection string
- **Pooling**: Recommended (Prisma Data Proxy or PgBouncer)
- **Region**: Choose closest to Vercel functions

---

## 🔄 Continuous Deployment

```
main branch     → Production deployment
other branches  → Preview deployment
Pull Requests   → Preview deployment + comment
```

**Automatic Triggers**:
- ✅ Push to main
- ✅ Push to any branch
- ✅ Pull request created
- ✅ Pull request updated

**Manual Triggers**:
- 🔄 Redeploy from dashboard
- 🔄 Vercel CLI: `vercel --prod`

---

## 📈 Scaling Behavior

### Traffic Increase
```
Low Traffic           Medium Traffic         High Traffic
    │                      │                      │
    │ 1-10 req/s          │ 100 req/s            │ 1000+ req/s
    │                      │                      │
    ▼                      ▼                      ▼
┌────────┐          ┌──────────────┐      ┌─────────────────┐
│ 1-2    │          │ Auto-scale   │      │ Massive         │
│ Instances│  ──►   │ to 10-20     │ ──►  │ Auto-scale      │
└────────┘          │ Instances    │      │ 100+ Instances  │
                    └──────────────┘      └─────────────────┘
```

**Cost**: Pay only for what you use
**Scaling**: Automatic, no configuration needed

---

## 🛡️ Security Layers

```
┌─────────────────────────────────────┐
│ 1. HTTPS (Automatic)                │
│    • SSL/TLS certificates           │
│    • Secure connections             │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│ 2. Vercel Firewall                  │
│    • DDoS protection                │
│    • Rate limiting                  │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│ 3. API Security                     │
│    • Helmet.js headers              │
│    • CORS policy                    │
│    • Rate limiters                  │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│ 4. Authentication                   │
│    • JWT tokens                     │
│    • HTTP-only cookies              │
│    • Role-based access              │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│ 5. Database Security                │
│    • Encrypted connections          │
│    • Parameterized queries          │
│    • Access control                 │
└─────────────────────────────────────┘
```

---

**This architecture provides**:
- ⚡ Fast global performance
- 🔄 Automatic scaling
- 🛡️ Enterprise security
- 📊 Built-in monitoring
- 🚀 Zero-downtime deployments
- 💰 Cost-effective pricing
